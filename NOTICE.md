# st_ipl — attribution

The framework code under `client/` (loader, observers, util, init) and the
declarative config under `shared/config.lua` are authored by the Strata
project and licensed alongside the rest of the framework.

The IPL data files under `data/` (per-DLC interior definitions, IPL names,
prop names, room hashes) are derived from
[bob74_ipl](https://github.com/Bob-74/bob74_ipl) by **Bob_74**, used under
the MIT License. The original copyright notice is preserved at
`LICENSE.bob74`.

The IPL identifier strings and prop names themselves are reverse-engineered
values from Rockstar's GTA V game files and are not copyrightable as such;
attribution is given for the curation work of compiling them into a usable
data set.

If you redistribute this resource, keep `LICENSE.bob74` and this NOTICE
file intact.
